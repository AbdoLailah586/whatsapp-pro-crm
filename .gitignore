# Node dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment & Secrets
.env
.env.local
.env.production

# Local Databases (PostgreSQL used in production) & per-tenant SQLite files,
# plus the auto-generated session-secret fallback file (data/.session_secret)
data/

# WhatsApp Auth & Uploads
auth_info/
src/public/uploads/*
!src/public/uploads/.gitkeep

# Scratch & Temporary Logs
scratch/
*.log
.DS_Store
Thumbs.db
.tempmediaStorage/
.user_uploaded/
